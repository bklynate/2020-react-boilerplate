import React from 'react';

const LandingPage = () => (
  <div>
    <h1>If you see this, then I think things are working as expected.</h1>
    <p>view the page-source to see if this is being SSR.</p>
  </div>
);

export default LandingPage;
